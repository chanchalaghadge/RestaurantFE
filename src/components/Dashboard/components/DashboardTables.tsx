import { orders, reservations } from "../data/dashboard.data";
function DashboardTables() { return <div className="dashboard-tables"><section className="dashboard-panel table-panel"><div className="dashboard-panel-heading"><div><h2>Recent Orders</h2><p>Latest orders from your customers</p></div><a href="#orders">View All →</a></div><div className="dashboard-table-wrap"><table><thead><tr><th>Order #</th><th>Customer</th><th>Items</th><th>Total</th><th>Status</th><th>Time</th></tr></thead><tbody>{orders.map((order) => <tr key={order.id}><td>{order.id}</td><td>{order.customer}</td><td>{order.items}</td><td>{order.total}</td><td><span className={`dashboard-status ${order.status.toLowerCase()}`}>{order.status}</span></td><td>{order.time}</td></tr>)}</tbody></table></div></section><section className="dashboard-panel table-panel"><div className="dashboard-panel-heading"><div><h2>Table Reservations</h2><p>Upcoming reservations</p></div><a href="#reservations">View All →</a></div><div className="dashboard-table-wrap"><table><thead><tr><th>Name</th><th>Party Size</th><th>Date &amp; Time</th><th>Status</th></tr></thead><tbody>{reservations.map((reservation) => <tr key={`${reservation.name}-${reservation.date}`}><td>{reservation.name}</td><td>{reservation.party}</td><td>{reservation.date}</td><td><span className={`dashboard-status ${reservation.status.toLowerCase()}`}>{reservation.status}</span></td></tr>)}</tbody></table></div></section></div>; }
export default DashboardTables;